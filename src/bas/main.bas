# Set line number increment to 1 in zmakebas. Data pointer manipulations depends on this.

define(VERSION, "V20.04.15")

include(settings.bas)
include(init.bas)
include(article.bas)
include(menu.bas)
include(contact.bas)
include(udg.bas)
include(disclaimer.bas)
include(user.bas)
include(menuitems.bas)
